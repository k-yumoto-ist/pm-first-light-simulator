export type IntroBriefItem = { label: string; value: string; className?: string; note?: string };

export function SimulatorIntro({ assignmentLabel, modeLabel, headline, emphasizedHeadline, description, rules, note, briefTitle, briefDescription, briefItems, actionLabel, onStart, exitLabel, onExit }: { assignmentLabel: string; modeLabel: string; headline: string; emphasizedHeadline: string; description: string; rules: Array<{ number: string; title: string; detail: string }>; note: string; briefTitle: string; briefDescription: string; briefItems: IntroBriefItem[]; actionLabel: string; onStart: () => void; exitLabel?: string; onExit?: () => void }) {
  return <main className="intro-shell"><div className="intro-glow" /><header className="brand"><span className="brand-mark">PM</span><span>PROJECT: FIRST LIGHT</span><small className="mode-badge">{modeLabel}</small>{onExit ? <button type="button" className="intro-exit" onClick={onExit}>{exitLabel ?? "戻る"}</button> : null}</header><section className="hero"><p className="eyebrow">{assignmentLabel}</p><h1>{headline}<br /><em>{emphasizedHeadline}</em></h1><p className="hero-copy">{description}</p><div className="intro-rules">{rules.map(rule => <div key={rule.number}><b>{rule.number}</b><span><strong>{rule.title}</strong><small>{rule.detail}</small></span></div>)}</div><p className="not-quiz">{note}</p><button className="primary large" onClick={onStart}>{actionLabel} <span>→</span></button></section><section className="brief-grid"><article className="brief-main"><span className="card-kicker">PROJECT BRIEF</span><h2>{briefTitle}</h2><p>{briefDescription}</p></article>{briefItems.map(item => <article key={item.label} className={item.className}><span>{item.label}</span><strong>{item.value}</strong>{item.note ? <small>{item.note}</small> : null}</article>)}</section></main>;
}
